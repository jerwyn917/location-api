import { TYPES } from '../default';

module.exports.default = {
    PlaceUpdateRequest: {
        key: {
            type: TYPES.string,
            description: '<description>',
        },
    },
};
