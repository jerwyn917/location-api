import { TYPES } from '../default';

module.exports.default = {
    PlaceDeleteRequest: {
        key: {
            type: TYPES.string,
            description: '<description>',
        },
    },
};
